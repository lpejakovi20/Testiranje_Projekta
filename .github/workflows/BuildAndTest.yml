name: Build And Test CI/CD Workflow

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    
    
    - name: Checkout
      uses: actions/checkout@v3.5.2
    - name: Setup Nuget
      uses: NuGet/setup-nuget@v1.2.0 
    - name: List files in folder
      run: pwd
    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v1.3.1
    - name: Navigate to Subfolder
      run: cd ${{ github.workspace }}/Software/E-ugostiteljstvo/E-ugostiteljstvo
    - name: List files in folder
      run: pwd
    - name: Restore NuGet Packages
      run: nuget restore ${{ github.workspace }}/Software/E-ugostiteljstvo/E-ugostiteljstvo/E-ugostiteljstvo.sln
    - name: Build solution 
      run: msbuild /p:platform="Any CPU" /p:configuration=Release
    - name: Run Unit Test
      run: dotnet test --configuration Release --filter BusinessLogicLayer_UnitTests
    - name: Run Integration Test
      run: dotnet test --configuration Release --filter BusinessLogicLayer_IntegrationTests
    - name: Navigate to startup project
      run: cd E-ugostiteljstvo
    - name: Publish application
      run: msbuild /t:publish /p:configuration=Release /p:publishdir=Publish/
    - name: Upload artifact
      uses: actions/upload-artifact@v3.1.2
      with:
        name: E-ugostiteljstvoSetup
        path: E-ugostiteljstvo/Publish/
      
      
      
      
      
